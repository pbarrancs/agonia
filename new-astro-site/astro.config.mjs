// astro.config.mjs
// Astro configuration for agonia website.
// output: 'static' produces a fully static build compatible with Firebase Hosting.

import { defineConfig } from 'astro/config';

export default defineConfig({
  output: 'static',
});
